# Core Science Stack
# CRITICAL: We must hold numpy back to <2.0 because OpenCV and Picamera2
# are not yet compatible with the Numpy 2.0 binary interface (ABI).
numpy>=1.26.0,<2.0.0
scipy>=1.11.0
pandas>=2.1.0

# Computer Vision & ML
# Since we are using a "Strict" venv, we MUST install opencv via pip here.
# The system apt version is invisible to us now.
opencv-python>=4.8.0
mediapipe>=0.10.9
picamera2>=0.3.19

# Utilities
click>=8.1.0
colorama>=0.4.6
matplotlib>=3.8.0

# Testing
pytest>=7.4.0